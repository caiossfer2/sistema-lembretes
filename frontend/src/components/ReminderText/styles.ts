import styled from "styled-components";

export const P = styled.p`
    font-weight: 700;
    color: #222;
`;

export const Wrapper = styled.div`
    display: flex;
    align-items: center;
    column-gap: 5px;
    margin-left: 50px;
`;